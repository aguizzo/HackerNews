<%= form_with(model: submission_ask) do |form| %>
  <% if submission_ask.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(submission_ask.errors.count, "error") %> prohibited this submission_ask from being saved:</h2>

      <ul>
        <% submission_ask.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tittle %>
    <%= form.text_field :tittle %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :punts %>
    <%= form.number_field :punts %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
